#ifndef TOOL_H
#define TOOL_H

#include "type.h"
#include <iostream>
#include <string.h>
namespace trinity_platform
{
//计算检校码
uint16_t getCks(const uint8_t* ptr,uint8_t len);

uint16_t bytes2int16(const char* bytes,bool littleOrder = true);

uint32_t bytes2int32(const char* bytes,bool littleOrder = true);

float bytes2float32(const char* bytes,bool littleOrder = true);

char* int162bytes(short length,bool littleOrder = true);

char* int322bytes(int length,bool littleOrder = true);

}


#endif//TOOL_H
